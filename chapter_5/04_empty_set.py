a = {} # here we have created an empty set but it's type is not considered of set its type is dictionary
print(type(a))

# an empty set can be created by using below syntax
b = set() # by this the type of b is (set)
print(type(b))
